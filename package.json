{
  "name": "blog_lite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "content:build": "node ./scripts/build-content-manifest.js",
    "predev": "npm run content:build",
    "dev": "vite",
    "prebuild": "npm run content:build",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.10"
  }
}
